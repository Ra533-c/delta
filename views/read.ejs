<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-16 flex-col flex justify-center items-center text-white gap-5">
        <a href="/" class="nav text-zinc-500 hover:text-blue-500">Back to home.</a>
        <h1 class="text-3xl tracking-tighter mb-1">All Users</h1>
        <div class="users flex flex-wrap gap-3 justify-center items-center">
        <% if(allUser.length === 0 ){ %>
            <p class="text-zinc-500">No users found.</p>
        <% } else { %>
            <% allUser.forEach(function(user){ %>
            <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                <div class="w-full h-72 rounded-lg bg-red-700 overflow-hidden">
                    <img class="w-full h-full overflow-hidden object-cover" src="<%= user.image %>" alt="">
                </div>
                <h3 class="text-xl mt-3 tracking-tighter "><%= user.name %></h3>
                <h5 class="text-zinc-500"><%= user.email %></h5>
                <div class="flex gap-3 mt-4">
                    <a class="text-zinc-500 hover:text-zinc-200 " href="/edit/<%= user._id %>">Edit User</a>
                    <a class="text-red-400 hover:text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                </div>
            </div>
            <% }) %>
        <% } %>
        </div>
    </div>
</body>

</html>